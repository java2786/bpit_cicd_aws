version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Building the React project...
      - npm run build

artifacts:
  files:
    - 'dist/**/*'
  base-directory: .  # Ensure the correct folder is used
